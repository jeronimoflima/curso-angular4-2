import { FiltroAarayImpuroPipe } from './filtro-aaray-impuro.pipe';

describe('FiltroAarayImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAarayImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
